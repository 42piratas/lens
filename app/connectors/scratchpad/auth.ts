/**
 * Scratchpad is a *local* connector — its data of record lives in the user's
 * browser (Phase 2) and will move to Supabase in Phase 3 once b02-06 lands.
 * No external auth is needed; this stub keeps the registry uniform.
 */
export const auth = {
  envVars: [] as const,
  setupDoc: "N/A — local data",
};
