import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "dracula-paper",
  label: "Dracula Paper",
  mode: "dark-paper",
  source: "Dracula — paper-on-dark variant. Canvas + sidebar on Dracula bg + current-line; cards on cream-white with Dracula bg as ink.",
};
