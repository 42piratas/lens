import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "rose-pine-paper",
  label: "Rosé Pine Paper",
  mode: "dark-paper",
  source: "Rosé Pine — paper-on-dark variant. Canvas + sidebar on Main (base / surface / overlay); cards on Dawn (base + overlay) with Dawn text (#575279) as ink.",
};
