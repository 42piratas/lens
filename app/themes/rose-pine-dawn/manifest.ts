import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "rose-pine-dawn",
  label: "Rosé Pine Dawn",
  mode: "light",
  source: "Mirrored from Rosé Pine Dawn (rosepinetheme.com)",
};
