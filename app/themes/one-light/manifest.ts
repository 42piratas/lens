import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "one-light",
  label: "One Light",
  mode: "light",
  source: "Mirrored from Atom One Light (atom/one-light-syntax)",
};
