import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "ayu-light",
  label: "Ayu Light",
  mode: "light",
  source: "Mirrored from Ayu by dempfi (github.com/dempfi/ayu)",
};
