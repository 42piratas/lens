import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "tokyo-night",
  label: "Tokyo Night",
  mode: "dark",
  source: "Mirrored from Tokyo Night Storm by enkia (github.com/enkia/tokyo-night-vscode-theme)",
};
