import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "light",
  label: "42labs Light",
  mode: "light",
  source: "42labs design system — canvas-warm + copper accent",
};
