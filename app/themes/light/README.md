# Light (default)

42labs design system canvas — warm cream surfaces with a copper accent. This theme is also the universal fallback (its selector is `:root, [data-theme="light"]`), so any theme that fails to define a token falls through to these values.

Source: `https://42labs.io/design`
