import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "monokai-light",
  label: "Monokai Light",
  mode: "light",
  source: "Monokai light inversion — warm off-white surface, classic accents darkened for AA contrast",
};
