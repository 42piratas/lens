import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "github-light",
  label: "GitHub Light",
  mode: "light",
  source: "Mirrored from GitHub Primer (github.com/primer/primitives)",
};
