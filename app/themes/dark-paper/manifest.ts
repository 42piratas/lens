import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "dark-paper",
  label: "42labs Dark Paper",
  mode: "dark-paper",
  source: "42labs DS — void canvas + panel sidebar with steel paper cards (paper-on-dark variant of `dark`)",
};
