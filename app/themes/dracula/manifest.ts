import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "dracula",
  label: "Dracula",
  mode: "dark",
  source: "Mirrored from Dracula by Zeno Rocha (draculatheme.com)",
};
