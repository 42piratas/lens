import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "one-dark-pro",
  label: "One Dark Pro",
  mode: "dark",
  source: "Mirrored from One Dark Pro by binaryify (Atom One Dark legacy)",
};
