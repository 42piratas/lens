import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "nord",
  label: "Nord",
  mode: "dark",
  source: "Mirrored from Nord by Arctic Ice Studio (nordtheme.com)",
};
