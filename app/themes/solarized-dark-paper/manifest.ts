import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "solarized-dark-paper",
  label: "Solarized Dark Paper",
  mode: "dark-paper",
  source: "Solarized by Ethan Schoonover — base03 canvas + base3 paper cards (selective-contrast variant)",
};
