import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "nord-paper",
  label: "Nord Paper",
  mode: "dark-paper",
  source: "Nord — paper-on-dark variant. Canvas + sidebar on Polar Night (nord0..nord3); cards on Snow Storm (nord4..nord6) with nord0 as ink.",
};
