import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "tokyo-night-day",
  label: "Tokyo Night Day",
  mode: "light",
  source: "Mirrored from Tokyo Night Day by enkia (github.com/enkia/tokyo-night-vscode-theme)",
};
