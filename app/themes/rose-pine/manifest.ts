import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "rose-pine",
  label: "Rosé Pine",
  mode: "dark",
  source: "Mirrored from Rosé Pine (rosepinetheme.com)",
};
