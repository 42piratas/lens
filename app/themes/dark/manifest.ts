import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "dark",
  label: "42labs Dark",
  mode: "dark",
  source: "42labs design system — void + mint accent",
};
