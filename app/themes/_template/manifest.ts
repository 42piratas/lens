// Reference template — copy this folder to `app/themes/<id>/`,
// then register the manifest in `app/themes/index.ts` and add the
// `@import` line in `app/themes/index.css`.
import type { ThemeManifest } from "../types";

export const manifest: ThemeManifest = {
  id: "_template",
  label: "Template",
  mode: "dark",
  source: "Replace with palette source / attribution.",
};
